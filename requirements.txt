fastapi>=0.111.0
uvicorn[standard]>=0.29.0
# 2.0 is required, not merely preferred: MCP Apps (the in-chat upload widget)
# does not exist before it.
mcp[cli]>=2.0.0,<3.0.0
# Google platforms
google-cloud-storage>=2.16.0
google-ads>=24.0.0
google-analytics-data>=0.18.0
google-analytics-admin>=0.22.0
google-auth>=2.29.0
google-auth-oauthlib>=1.2.0
google-api-python-client>=2.127.0
# Meta and generic HTTP
requests>=2.31.0
httpx>=0.27.0
# Creative generation
openai>=1.78.0
# Local credential store encryption
cryptography>=42.0.0
# Config
python-dotenv>=1.0.0
# PDF / chart rendering in composite reports
matplotlib>=3.8.0
numpy>=1.26.0
# Image normalisation before Meta upload (tools/meta_ads.py). matplotlib pulls
# Pillow in transitively, but this code path depends on it directly -- without
# an explicit pin the import can silently disappear and the uploader quietly
# degrades to passing PNGs through, which Meta rejects.
pillow>=10.0.0
